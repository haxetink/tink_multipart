sudo: required
dist: trusty

language: haxe

os:
  - linux
  
haxe:
  - "3.2.1"
    
install:
  - haxelib install travix
  - haxelib run travix install
  - npm install busboy 

script:
  - haxelib run travix node
